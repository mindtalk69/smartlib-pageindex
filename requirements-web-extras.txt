# Optional heavy dependencies for web container when local embeddings are required
# Install with: docker build --build-arg INSTALL_WEB_EXTRAS=true ...
# Uses CPU-only PyTorch from https://download.pytorch.org/whl/cpu

# HuggingFace embedding stack (CPU only)
--extra-index-url https://download.pytorch.org/whl/cpu
sentence-transformers==2.6.0
torch==2.2.2+cpu
torchvision==0.17.2+cpu
