# ============================================================================
# WORKER OPTIMIZED REQUIREMENTS.TXT - Target: ~500-600MB for BASIC edition
# ML and document processing container with full RAG capabilities
# sqlite-vec for vector storage (no PostgreSQL/PGVector)
# ============================================================================

# Core Web Framework (needed for task processing)
Flask==3.1.0
Flask-Login==0.6.3
Flask-Migrate==4.1.0
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
gunicorn>=20.1.0
Werkzeug==3.1.3

# Celery Task Queue (full worker)
celery==5.5.2
redis==5.0.1

# Database (SQLite only)
SQLAlchemy==2.0.39
alembic==1.15.2

# OpenAI API (primary embedding method)
openai==1.100.2
tiktoken==0.9.0
langchain-openai==0.2.10

# LangChain Core
langchain>=0.3.18
langchain-community>=0.3.17
langgraph>=0.2.35
langchain-experimental>=0.0.65
langgraph-prebuilt>=0.1.8
langgraph-checkpoint>=2.0.25
langgraph-supervisor>=0.0.19
langgraph-sdk>=0.1.66

# Vector Store - sqlite-vec (embedded vectors in SQLite)
sqlite-vec>=0.1.1

# Local Embeddings - Qwen3-Embedding-0.6B via API (no local torch needed)
# Remove torch/sentence-transformers if using API-only embeddings
langchain-huggingface>=0.1.2

# HTTP & Utilities (minimal)
httpx==0.28.1
requests==2.32.3
python-dotenv==1.0.1
pydantic==2.11.4
lark

# Document Processing (optimized for worker)
docling-core~=2.29
docling~=2.12
langchain-docling
rapidocr-onnxruntime
PyMuPDF
pdfminer.six

# Agent tooling
selenium
webdriver-manager
folium
geopy
matplotlib
numpy<2.0.0
scipy>=1.14.1,<2.0.0
scikit-learn<1.5.0

# Azure Document Intelligence (optional - for OCR via API)
azure-ai-documentintelligence

# ============================================================================
# NOTES:
# ============================================================================
# - Using Qwen3-Embedding-0.6B via API (no local torch/sentence-transformers)
# - sqlite-vec for vector storage (embedded in SQLite, no PostgreSQL)
# - RapidOCR for local OCR processing
# - Docling for document parsing
#
# Expected size: ~400-500MB (without local embedding models)
# - Supports: Qwen3-Embedding-0.6B (API)
# - Supports: Document processing with RapidOCR
#
# Worker-only responsibilities:
# - Document ingestion and processing
# - Embedding generation via API
# - Background task processing (Celery)
# - Vector store operations (sqlite-vec)
# ============================================================================
