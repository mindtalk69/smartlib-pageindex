# ============================================================================
# WORKER OPTIMIZED REQUIREMENTS.TXT - Target: ~600-700MB for Azure B1 deployment
# ML and document processing container with full RAG capabilities
# ============================================================================

# Core Web Framework (needed for task processing)
Flask==3.1.0
Flask-Login==0.6.3
Flask-Migrate==4.1.0
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
gunicorn>=20.1.0
Werkzeug==3.1.3

# Celery Task Queue (full worker)
celery==5.5.2
redis==5.0.1

# Database (SQLite only)
SQLAlchemy==2.0.39
alembic==1.15.2

# OpenAI (core AI) - keep for API fallback
openai==1.100.2
tiktoken==0.9.0
langchain-openai==0.2.10
langchain>=0.3.18
langchain-community>=0.3.17
langchain-chroma>=0.1.4
langgraph>=0.2.35
langchain-postgres>=0.0.6
langchain-experimental>=0.0.65
langgraph-prebuilt>=0.1.8
langgraph-checkpoint>=2.0.25
langgraph-supervisor>=0.0.19
langgraph-sdk>=0.1.66
chromadb>=0.5.23
langchain-huggingface>=0.1.2
lark
pgvector


# Essential ML (CPU-only) - for HuggingFace embeddings
torch>=2.2.2,<3.0.0
sentence-transformers>=2.6.0,<2.7.0
numpy<2.0.0
scipy>=1.14.1,<2.0.0
scikit-learn<1.5.0

# HTTP & Utilities (minimal)
httpx==0.28.1
requests==2.32.3
python-dotenv==1.0.1
pydantic==2.11.4

# Document Processing (optimized for worker)
# Using compatible versions with focus on RapidOCR support
docling-core~=2.29
docling~=2.12
langchain-docling
PyMuPDF
pdfminer.six
numpy<2.0.0

# Agent tooling
selenium
webdriver-manager
folium
geopy
matplotlib

# AZURE INTEGRATIONS (if needed) , /admin/ocr
azure-ai-documentintelligence
msal

# ============================================================================
# WORKER OPTIMIZATIONS:
# ============================================================================
# - CPU-only PyTorch (no CUDA) - ~400MB vs 800MB
# - Optimized sentence-transformers version (smaller dependencies)
# - SQLite only (no PostgreSQL)
# - Minimal scientific packages
# - Docling with RapidOCR focus (lighter than EasyOCR)
#
# Expected size: ~600-700MB
# - Supports: all-MiniLM-L6-v2 (90MB) + text-embedding-3-small (API)
# - Supports: BAAI/bge-m3 (1.5GB) - full PyTorch ecosystem
# - Supports: Document processing with OCR
# - Supports: Full RAG pipeline
#
# Worker-only responsibilities:
# - Document ingestion and processing
# - Local embedding generation (HuggingFace models)
# - Background task processing
# - Vector store operations
# ============================================================================
