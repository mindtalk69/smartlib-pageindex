---
phase: 07-backend-llm-providers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [main_fastapi.py]
autonomous: true
requirements: [PROV-05, PROV-06, PROV-07, PROV-08]
gap_closure: true

must_haves:
  truths:
    - "Admin can test provider connectivity and update health status"
    - "Admin can discover available models from provider"
    - "Admin can update provider priorities (priority ordering)"
    - "Provider health status is tracked and retrievable"
  artifacts:
    - path: "main_fastapi.py"
      provides: "LLM Provider action endpoints (test, discover, priority)"
      exports: ["POST /api/v1/admin/providers/{id}/test", "POST /api/v1/admin/providers/{id}/discover-models", "POST /api/v1/admin/providers/priority"]
  key_links:
    - from: "main_fastapi.py"
      to: "LLMProvider health fields"
      via: "database update"
      pattern: "last_health_check|health_status|error_message"
    - from: "frontend/src/admin-app/src/components/providers/ProviderList.tsx"
      to: "main_fastapi.py"
      via: "API calls"
      pattern: "testProvider|discoverModels|updatePriorities"
---

<objective>
Implement FastAPI action endpoints for LLM provider management (test connectivity, discover models, update priorities, health status).

Purpose: Close gaps PROV-05 through PROV-08 by porting Flask action endpoints from modules/admin_providers.py to FastAPI.

Output: Four new FastAPI endpoints in main_fastapi.py for provider actions and health monitoring.
</objective>

<execution_context>
@/home/mlk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/mlk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md

Source Flask action endpoints to port (modules/admin_providers.py):
- Lines 237-259: test_provider() - POST /admin/providers/<id>/test
- Lines 280-300: discover_models() - POST /admin/providers/<id>/discover-models
- Lines 303-327: update_priorities() - POST /admin/providers/api/providers/priority
- Lines 32-40, 251-253: Health status tracking (last_health_check, health_status, error_message)

Helper functions needed (from modules/llm_provider_utils.py):
- test_provider_connection(provider) -> dict with status, error, message
- discover_provider_models(provider) -> list of model dicts

Key existing FastAPI patterns (main_fastapi.py):
- Lines 184-204: LLMProvider model with health fields
- Lines 243-258: Provider CRUDRouter registration
- Lines 910-948: POST pattern for admin action endpoints
</interfaces>

Flask patterns to match:
- test_provider: Updates last_health_check, health_status, error_message on provider
- discover_models: Returns list of models without creating them
- update_priorities: Array of {id, priority} objects, batch updates all
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test provider connectivity endpoint (PROV-05)</name>
  <files>main_fastapi.py</files>
  <action>
    Add POST endpoint at `/api/v1/admin/providers/{provider_id}/test` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates provider_id exists (404 if not found)
    3. Calls `test_provider_connection(provider)` helper function
    4. Updates provider health status fields:
       - last_health_check = current UTC datetime
       - health_status = result['status'] (e.g., 'healthy', 'error')
       - error_message = result.get('error') or result.get('message')
    5. Returns test result with status, message, and error details
    
    Request: No body required (provider_id in path)
    
    Response:
    ```json
    {
      "success": true,
      "status": "healthy",
      "message": "Connection successful",
      "provider_id": 1,
      "last_health_check": "2026-02-27T..."
    }
    ```
    
    Helper function signature needed:
    ```python
    def test_provider_connection(provider: LLMProvider) -> dict:
        """Test provider connectivity and return result dict."""
        # Implementation in modules/llm_provider_utils.py
        return {'status': 'healthy', 'message': 'Success', 'error': None}
    ```
    
    Error handling:
    - 404 if provider not found
    - 500 on connection test failure (returns error status)
    - Database commit after health update
    
    Follow Flask pattern from modules/admin_providers.py lines 237-259.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8001/api/v1/admin/providers/1/test -H "Authorization: Bearer TOKEN" | jq '.status'</automated>
  </verify>
  <done>Endpoint tests connection, updates health status fields, returns test result with status and message</done>
</task>

<task type="auto">
  <name>Task 2: Add discover models endpoint (PROV-06)</name>
  <files>main_fastapi.py</files>
  <action>
    Add POST endpoint at `/api/v1/admin/providers/{provider_id}/discover-models` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates provider_id exists (404 if not found)
    3. Calls `discover_provider_models(provider)` helper function
    4. Returns list of discovered models with provider info
    
    Request: No body required (provider_id in path)
    
    Response:
    ```json
    {
      "success": true,
      "provider": {
        "id": 1,
        "name": "Azure OpenAI",
        ...
      },
      "models": [
        {
          "model_id": "gpt-4",
          "name": "GPT-4",
          "provider": "azure_openai"
        },
        {
          "model_id": "gpt-35-turbo",
          "name": "GPT-3.5 Turbo",
          "provider": "azure_openai"
        }
      ]
    }
    ```
    
    Helper function signature needed:
    ```python
    def discover_provider_models(provider: LLMProvider) -> list:
        """Discover available models from provider and return list."""
        # Implementation in modules/llm_provider_utils.py
        return [{'model_id': '...', 'name': '...', 'provider': provider.provider_type}]
    ```
    
    Error handling:
    - 404 if provider not found
    - 500 if model discovery fails (returns error in response)
    - Returns empty models list if discovery succeeds but no models found
    
    Follow Flask pattern from modules/admin_providers.py lines 280-300.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8001/api/v1/admin/providers/1/discover-models -H "Authorization: Bearer TOKEN" | jq '.models'</automated>
  </verify>
  <done>Endpoint calls discovery helper, returns list of available models with provider info</done>
</task>

<task type="auto">
  <name>Task 3: Add update provider priorities endpoint (PROV-07)</name>
  <files>main_fastapi.py</files>
  <action>
    Add POST endpoint at `/api/v1/admin/providers/priority` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Accepts array of {id, priority} objects in request body
    3. Updates priority for each provider in the array
    4. Commits all changes in single transaction
    5. Returns success confirmation
    
    Request body:
    ```json
    {
      "priorities": [
        {"id": 1, "priority": 0},
        {"id": 2, "priority": 1},
        {"id": 3, "priority": 2}
      ]
    }
    ```
    
    Response:
    ```json
    {
      "success": true,
      "message": "Priorities updated"
    }
    ```
    
    Update logic:
    - Iterate through priorities array
    - For each {id, priority}, query LLMProvider by id
    - Update priority field if provider exists
    - db.session.commit() after all updates
    
    Error handling:
    - 400 if priorities array missing or empty
    - 400 if item missing id or priority
    - 500 on database errors
    
    Follow Flask pattern from modules/admin_providers.py lines 303-327.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8001/api/v1/admin/providers/priority -H "Authorization: Bearer TOKEN" -H "Content-Type: application/json" -d '{"priorities":[{"id":1,"priority":0}]}' | jq '.success'</automated>
  </verify>
  <done>Endpoint accepts priority array, updates all providers, returns success</done>
</task>

<task type="auto">
  <name>Task 4: Add provider health status to list endpoint (PROV-08)</name>
  <files>main_fastapi.py</files>
  <action>
    Enhance existing GET /api/v1/admin/providers endpoint to include health status fields:
    1. Extend response to include health status fields in each provider object
    2. Ensure last_health_check, health_status, error_message are included
    3. Format datetime fields as ISO strings for JSON compatibility
    
    Enhanced provider object:
    ```json
    {
      "id": 1,
      "name": "Azure OpenAI",
      "provider_type": "azure_openai",
      "base_url": "https://...",
      "is_active": true,
      "is_default": false,
      "priority": 0,
      "config": {},
      "last_health_check": "2026-02-27T10:00:00Z",
      "health_status": "healthy",
      "error_message": null,
      "created_at": "2026-02-27T08:00:00Z",
      "updated_at": "2026-02-27T09:00:00Z"
    }
    ```
    
    Health field meanings:
    - last_health_check: ISO datetime string or null
    - health_status: 'healthy' | 'degraded' | 'error' | 'unknown' | null
    - error_message: Error string if健康check failed, null if successful
    
    Implementation: Modify existing list endpoint to include all provider fields
    - Use provider.to_dict() or manually construct dict with all fields
    - Ensure datetime serialization (isoformat()) for health fields
    - Maintain backwards compatibility with existing endpoint
    
    Enhancement to existing endpoint in main_fastapi.py (Task 1 of this plan).
  </action>
  <verify>
    <automated>curl "http://localhost:8001/api/v1/admin/providers" | jq '.data.items[0].health_status'</automated>
  </verify>
  <done>Provider list includes all health status fields (last_health_check, health_status, error_message)</done>
</task>

</tasks>

<verification>
All four action endpoints respond correctly:
- POST /api/v1/admin/providers/{id}/test returns 200 with status and health update
- POST /api/v1/admin/providers/{id}/discover-models returns 200 with models list
- POST /api/v1/admin/providers/priority returns 200 with success
- GET /api/v1/admin/providers includes health status fields in each provider
- Health status updated after test (last_health_check, health_status, error_message)
- Non-admin users receive 403 Forbidden
</verification>

<success_criteria>
1. Four new FastAPI action endpoints exist in main_fastapi.py
2. Health status fields (last_health_check, health_status, error_message) present on provider objects
3. Health status updated after connectivity test
4. Model discovery returns list of available models without creating records
5. Priority update accepts array of {id, priority} and updates all providers
6. Helper functions test_provider_connection() and discover_provider_models() implemented
7. datetime fields properly serialized to ISO strings for JSON responses
</success_criteria>

<output>
After completion, create `.planning/phases/07-backend-llm-providers/07-02-SUMMARY.md`
</output>
