---
phase: 07-backend-llm-providers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [main_fastapi.py]
autonomous: true
requirements: [PROV-01, PROV-02, PROV-03, PROV-04]
gap_closure: true

must_haves:
  truths:
    - "Admin can list all LLM providers with priority ordering"
    - "Admin can add new providers with validation"
    - "Admin can edit existing provider details"
    - "Admin can delete providers (only if no associated models)"
  artifacts:
    - path: "main_fastapi.py"
      provides: "LLM Provider CRUD endpoints"
      exports: ["GET /api/v1/admin/providers", "POST /api/v1/admin/providers", "PUT /api/v1/admin/providers/{id}", "DELETE /api/v1/admin/providers/{id}"]
  key_links:
    - from: "main_fastapi.py"
      to: "LLMProvider model"
      via: "database query"
      pattern: "select\\(LLMProvider\\)"
    - from: "frontend/src/admin-app/src/hooks/useProviders.ts"
      to: "main_fastapi.py"
      via: "API calls"
      pattern: "getProviders|addProvider|updateProvider|deleteProvider"
---

<objective>
Implement FastAPI CRUD endpoints for LLM provider management (list, add, edit, delete).

Purpose: Close gaps PROV-01 through PROV-04 by porting Flask endpoints from modules/admin_providers.py to FastAPI.

Output: Four new FastAPI endpoints in main_fastapi.py for provider management with admin authentication.
</objective>

<execution_context>
@/home/mlk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/mlk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md

Source Flask endpoints to port (modules/admin_providers.py):
- Lines 44-53: api_list_providers() - GET /admin/providers/api/providers (JSON list)
- Lines 56-126: add_provider() - POST /admin/providers/add (create)
- Lines 129-197: edit_provider() - POST /admin/providers/<id>/edit (update)
- Lines 200-234: delete_provider() - POST/DELETE /admin/providers/<id>/delete

Key existing FastAPI patterns (main_fastapi.py):
- Lines 184-204: LLMProvider model definition in modules/models.py
- Lines 243-258: Provider CRUDRouter registration at /api/v1/providers
- Lines 910-914: toggle_active_status - POST pattern for admin actions
</interfaces>

Tasks match Flask behavior from modules/admin_providers.py:
- List providers ordered by priority then name (line 49-50)
- Add requires name + provider_type, validates uniqueness (lines 67-84)
- Edit validates name uniqueness on change (lines 144-151)
- Delete checks for associated models (lines 207-214)
- All endpoints require admin authentication
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add list providers endpoint (PROV-01)</name>
  <files>main_fastapi.py</files>
  <action>
    Add GET endpoint at `/api/v1/admin/providers` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Queries LLMProvider table ordered by priority then name
    3. Returns paginated list with all provider fields
    4. Handles empty results gracefully (empty list, total=0)
    
    Response format:
    ```json
    {
      "success": true,
      "data": {
        "items": [
          {
            "id": 1,
            "name": "Azure OpenAI",
            "provider_type": "azure_openai",
            "base_url": "https://...",
            "is_active": true,
            "is_default": false,
            "priority": 0,
            "config": {},
            "last_health_check": "2026-02-27T...",
            "health_status": "healthy",
            "error_message": null,
            "created_at": "2026-02-27T...",
            "updated_at": "2026-02-27T..."
          }
        ],
        "total": 1
      }
    }
    ```
    
    Match Flask behavior from modules/admin_providers.py lines 44-53:
    - Return providers as JSON list (not HTML template)
    - Order by priority, then name
    - Include all provider fields from to_dict() method
  </action>
  <verify>
    <automated>curl "http://localhost:8001/api/v1/admin/providers" -H "Authorization: Bearer TOKEN" | jq '.data.items'</automated>
  </verify>
  <done>Endpoint returns list of all providers ordered by priority with complete provider details</done>
</task>

<task type="auto">
  <name>Task 2: Add create provider endpoint (PROV-02)</name>
  <files>main_fastapi.py</files>
  <action>
    Add POST endpoint at `/api/v1/admin/providers` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates required fields: name (non-empty), provider_type (non-empty)
    3. Checks for duplicate name (returns 400 if exists)
    4. Parses optional config JSON field
    5. Creates provider with default values for optional fields
    6. Returns created provider with 201 status
    
    Request body:
    ```json
    {
      "name": "Azure OpenAI",
      "provider_type": "azure_openai",
      "base_url": "https://...",
      "api_key": "sk-...",
      "is_active": true,
      "priority": 0,
      "config": {}
    }
    ```
    
    Response:
    ```json
    {
      "success": true,
      "provider": {
        "id": 1,
        "name": "Azure OpenAI",
        ...
      }
    }
    ```
    
    Error handling:
    - 400 if name or provider_type missing
    - 400 if provider with same name exists
    - 400 if config JSON is invalid
    - 500 on database errors
    
    Follow Flask pattern from modules/admin_providers.py lines 56-126.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8001/api/v1/admin/providers -H "Authorization: Bearer TOKEN" -H "Content-Type: application/json" -d '{"name":"Test Provider","provider_type":"test"}' | jq '.success'</automated>
  </verify>
  <done>Endpoint creates provider with validation, prevents duplicates, returns created provider with 201</done>
</task>

<task type="auto">
  <name>Task 3: Add update provider endpoint (PROV-03)</name>
  <files>main_fastapi.py</files>
  <action>
    Add PUT/PATCH endpoint at `/api/v1/admin/providers/{provider_id}` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates provider_id exists (404 if not found)
    3. Updates only provided fields (partial update)
    4. Validates name uniqueness when name changes (400 if duplicate)
    5. Parses optional config JSON field
    6. Updates updated_at timestamp
    7. Returns updated provider object
    
    Request body (all fields optional):
    ```json
    {
      "name": "Updated Name",
      "base_url": "https://...",
      "is_active": false,
      "priority": 1,
      "config": {"new_key": "value"}
    }
    ```
    
    Response:
    ```json
    {
      "success": true,
      "provider": {
        "id": 1,
        "name": "Updated Name",
        ...
      }
    }
    ```
    
    Error handling:
    - 404 if provider not found
    - 400 if name already exists (different provider)
    - 400 if config JSON is invalid
    - 500 on database errors
    
    Follow Flask pattern from modules/admin_providers.py lines 129-197.
  </action>
  <verify>
    <automated>curl -X PUT http://localhost:8001/api/v1/admin/providers/1 -H "Authorization: Bearer TOKEN" -H "Content-Type: application/json" -d '{"is_active":false}' | jq '.success'</automated>
  </verify>
  <done>Endpoint updates provider fields with validation, prevents duplicate names, returns updated provider</done>
</task>

<task type="auto">
  <name>Task 4: Add delete provider endpoint (PROV-04)</name>
  <files>main_fastapi.py</files>
  <action>
    Add DELETE endpoint at `/api/v1/admin/providers/{provider_id}` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates provider_id exists (404 if not found)
    3. Checks for associated models (returns 400 if models exist)
    4. Deletes provider from database
    5. Returns success confirmation
    
    Response:
    ```json
    {
      "success": true,
      "message": "Provider deleted successfully"
    }
    ```
    
    Error handling:
    - 404 if provider not found
    - 400 if provider has associated models (prevents orphan models)
    - 500 on database errors
    
    Follow Flask pattern from modules/admin_providers.py lines 200-234:
    - Check provider.models.count() before deletion
    - Return error if models exist with count in message
    - db.session.delete() + db.session.commit()
  </action>
  <verify>
    <automated>curl -X DELETE http://localhost:8001/api/v1/admin/providers/1 -H "Authorization: Bearer TOKEN" | jq '.success'</automated>
  </verify>
  <done>Endpoint deletes provider only if no associated models, returns success confirmation</done>
</task>

</tasks>

<verification>
All four endpoints respond correctly:
- GET /api/v1/admin/providers returns 200 with provider list
- POST /api/v1/admin/providers returns 201 with created provider
- PUT /api/v1/admin/providers/{id} returns 200 with updated provider
- DELETE /api/v1/admin/providers/{id} returns 200 with success
- List ordered by priority then name
- Create/update validate name uniqueness
- Delete prevents removal if models exist
- Non-admin users receive 403 Forbidden
</verification>

<success_criteria>
1. Four new FastAPI endpoints exist in main_fastapi.py for provider CRUD
2. Provider name uniqueness validated on create and update
3. Delete prevents provider removal if associated models exist
4. All endpoints require admin authentication via get_current_admin_user
5. Empty provider list handled gracefully (returns [], total=0)
6. Config JSON field parsed correctly (dict or empty object)
7. Updated_at timestamp updated on edit operations
</success_criteria>

<output>
After completion, create `.planning/phases/07-backend-llm-providers/07-01-SUMMARY.md`
</output>
