---
phase: 06-backend-user-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [main_fastapi.py]
autonomous: true
requirements: [USER-04, USER-05, USER-07]
gap_closure: true

must_haves:
  truths:
    - "Admin can toggle user admin status via API endpoint"
    - "Admin can toggle user active/disabled status via API endpoint"
    - "Admin can delete user accounts via API endpoint"
    - "Self-modification (admin status, disable, delete own account) is prevented"
  artifacts:
    - path: "main_fastapi.py"
      provides: "FastAPI endpoints for user actions"
      exports: ["POST /api/v1/admin/users/{user_id}/toggle-admin", "POST /api/v1/admin/users/{user_id}/toggle-active", "DELETE /api/v1/admin/users/{user_id}"]
  key_links:
    - from: "main_fastapi.py"
      to: "User model"
      via: "database query"
      pattern: "select\\(User\\)\\.where\\(User\\.user_id =="
    - from: "frontend/src/admin-app/src/hooks/useUsers.ts"
      to: "main_fastapi.py"
      via: "API calls"
      pattern: "toggleAdmin|toggleActive|deleteUser"
---

<objective>
Implement FastAPI endpoints for user action operations (toggle admin, toggle active, delete user).

Purpose: Close gaps USER-04, USER-05, USER-07 by porting Flask API endpoints from modules/admin_users.py to FastAPI.

Output: Three new FastAPI endpoints in main_fastapi.py with proper admin authentication, validation, and error handling.
</objective>

<execution_context>
@/home/mlk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/mlk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md

Source Flask endpoints to port (modules/admin_users.py):
- Lines 289-461: API endpoints for user management

Key existing FastAPI patterns (main_fastapi.py):
- Lines 473-546: Existing admin user endpoints (list, get, update, reset-password)
- Lines 17-20: Auth dependencies (get_current_user, get_current_admin_user)
- Lines 6-11: SQLModel imports (User model, select)

Interfaces:

From main_fastapi.py existing admin endpoints:
```python
@app.get("/api/v1/admin/users")
def list_admin_users(
    db = Depends(get_db),
    current_user: User = Depends(get_current_admin_user),
    skip: int = 0,
    limit: int = 100,
):
```

From modules/models.py - User model:
```python
class User(SQLModel, table=True):
    __tablename__ = 'users'
    user_id: str = Field(primary_key=True)
    username: str = Field(index=True)
    auth_provider: str = Field(index=True)
    email: Optional[str] = Field(default=None)
    password_hash: Optional[str] = Field(default=None)
    is_admin: bool = Field(default=False)
    is_disabled: bool = Field(default=False)
    created_at: datetime = Field(default_factory=datetime.utcnow, ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add toggle admin status endpoint (USER-04)</name>
  <files>main_fastapi.py</files>
  <action>
    Add POST endpoint at `/api/v1/admin/users/{user_id}/toggle-admin` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates user_id exists in database using `select(User).where(User.user_id == user_id)`
    3. Prevents self-modification (if user_id == current_user.user_id, return 400 error)
    4. Toggles `user.is_admin` field (True ↔ False)
    5. Commits change to database
    6. Returns JSON response: `{"success": True, "data": {"user_id": "...", "is_admin": true/false}}`
    
    Error handling:
    - 403 if current_user is not admin
    - 404 if user_id not found
    - 400 if attempting to modify own admin status
    - 500 on database errors
    
    Follow existing patterns from lines 508-546 in main_fastapi.py (update_admin_user endpoint).
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8001/api/v1/admin/users/test-user-id/toggle-admin -H "Authorization: Bearer TOKEN" -w "\nHTTP Status: %{http_code}\n"</automated>
  </verify>
  <done>Endpoint accepts POST requests, toggles is_admin field, returns updated user data with proper error codes</done>
</task>

<task type="auto">
  <name>Task 2: Add toggle active status endpoint (USER-05)</name>
  <files>main_fastapi.py</files>
  <action>
    Add POST endpoint at `/api/v1/admin/users/{user_id}/toggle-active` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates user_id exists in database
    3. Prevents self-modification (if user_id == current_user.user_id, return 400 error with message "You cannot disable your own account")
    4. Toggles `user.is_disabled` field (True ↔ False)
    5. Commits change to database
    6. Returns JSON response: `{"success": True, "data": {"user_id": "...", "is_disabled": true/false}}`
    
    Error handling:
    - 403 if current_user is not admin
    - 404 if user_id not found
    - 400 if attempting to modify own account
    - 500 on database errors
    
    Match Flask endpoint behavior from modules/admin_users.py lines 87-110.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8001/api/v1/admin/users/test-user-id/toggle-active -H "Authorization: Bearer TOKEN" -w "\nHTTP Status: %{http_code}\n"</automated>
  </verify>
  <done>Endpoint accepts POST requests, toggles is_disabled field, prevents self-disable, returns updated user data</done>
</task>

<task type="auto">
  <name>Task 3: Add delete user endpoint (USER-07)</name>
  <files>main_fastapi.py</files>
  <action>
    Add DELETE endpoint at `/api/v1/admin/users/{user_id}` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates user_id exists in database
    3. Prevents self-deletion (if user_id == current_user.user_id, return 400 error with message "Cannot delete your own account")
    4. Deletes user record from database using `db.delete(user)` and `db.commit()`
    5. Returns JSON response: `{"success": True}`
    
    Error handling:
    - 403 if current_user is not admin
    - 404 if user_id not found
    - 400 if attempting to delete own account
    - 500 on database errors (with rollback)
    
    Follow pattern from Flask endpoint at modules/admin_users.py lines 409-430.
  </action>
  <verify>
    <automated>curl -X DELETE http://localhost:8001/api/v1/admin/users/test-user-id -H "Authorization: Bearer TOKEN" -w "\nHTTP Status: %{http_code}\n"</automated>
  </verify>
  <done>Endpoint accepts DELETE requests, removes user from database, prevents self-deletion, returns success response</done>
</task>

</tasks>

<verification>
All three endpoints respond correctly:
- POST /api/v1/admin/users/{id}/toggle-admin returns 200 with updated is_admin status
- POST /api/v1/admin/users/{id}/toggle-active returns 200 with updated is_disabled status
- DELETE /api/v1/admin/users/{id} returns 200 with success confirmation
- All endpoints return 403 for non-admin users
- All endpoints return 400 when user attempts self-modification
- All endpoints return 404 for non-existent user IDs
</verification>

<success_criteria>
1. Three new FastAPI endpoints exist in main_fastapi.py
2. Endpoints use proper admin authentication via `get_current_admin_user`
3. Self-modification prevention is implemented for all three actions
4. Database transactions are properly committed/rolled back
5. Response format matches frontend expectations (success/data/error structure)
6. OpenAPI docs at /docs show new endpoints under admin user operations
</success_criteria>

<output>
After completion, create `.planning/phases/06-backend-user-actions/06-01-SUMMARY.md`
</output>
