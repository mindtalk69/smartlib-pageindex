---
phase: 08-backend-llm-model-language
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: [main_fastapi.py]
autonomous: true
requirements: [MODEL-03, MODEL-04]

must_haves:
  truths:
    - "Admin can edit model configuration"
    - "Admin can delete model"
  artifacts:
    - path: "main_fastapi.py"
      provides: "Model Config update and delete endpoints"
      exports: ["POST /api/v1/admin/models/edit/{id}", "POST /api/v1/admin/models/delete/{id}"]
  key_links:
    - from: "main_fastapi.py"
      to: "ModelConfig model"
      via: "database query"
      pattern: "select\\(ModelConfig\\)"
    - from: "main_fastapi.py"
      to: "modules/llm_utils.py"
      via: "validation helpers"
      pattern: "is_streaming_supported_for_deployment|validate_temperature_for_deployment"
---

<objective>
Implement FastAPI update and delete endpoints for Model Config management.

Purpose: Close gaps MODEL-03 and MODEL-04 by porting Flask edit and delete endpoints to FastAPI.

Output: Two new FastAPI endpoints in main_fastapi.py for model update and delete.
</objective>

<execution_context>
@/home/mlk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/mlk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-llm-model-language/08-RESEARCH.md
@.planning/phases/08-backend-llm-model-language/08-01-PLAN.md

Prerequisite: Plan 08-01 must be complete (schemas exist, list/create endpoints work).

Source Flask endpoints to port (modules/admin_models.py):
- Lines 184-271: edit_model() - POST /admin/models/edit/<id> (update with validation)
- Lines 274-286: delete_model_route() - POST/DELETE /admin/models/delete/<id>

Key validation logic from modules/admin_models.py:
- Partial update using model_update with exclude_unset=True
- Validates deployment configuration if deployment_name, temperature, or streaming changes
- Name uniqueness check on name change
- Clears other defaults if is_default=true
- Delete checks AppSettings for multimodal_model_id reference (warn but allow)
</context>

<interfaces>
Frontend expects (from frontend/src/admin-app/src/hooks/useModels.ts):

POST /api/v1/admin/models/edit/{id}
  Request: { name?, deployment_name?, provider_id?, temperature?, streaming?, description?, is_default? }
  Response: { success: true, model: ModelConfig }

POST /api/v1/admin/models/delete/{id}
  Response: { success: true, message: string }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add update model endpoint (MODEL-03)</name>
  <files>main_fastapi.py</files>
  <action>
    Add POST endpoint at `/api/v1/admin/models/edit/{model_id}` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates model_id exists (404 if not found)
    3. Updates only provided fields (partial update using model_update with exclude_unset=True)
    4. Validates deployment configuration if deployment_name, temperature, or streaming changes
    5. Validates name uniqueness when name changes (400 if duplicate)
    6. Clears other defaults if is_default=true
    7. Returns updated model object

    Request body (all fields optional):
    ```json
    {
      "name": "Updated Name",
      "temperature": 0.5,
      "streaming": false,
      "is_default": true
    }
    ```

    Error handling:
    - 404 if model not found
    - 400 if deployment validation fails
    - 400 if name already exists (different model)
    - 500 on database errors

    Follow Flask pattern from modules/admin_models.py lines 184-271.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8001/api/v1/admin/models/edit/1 -H "Authorization: Bearer TOKEN" -H "Content-Type: application/json" -d '{"temperature":0.5}' | jq '.success'</automated>
  </verify>
  <done>Endpoint updates model fields with validation, prevents duplicate names, returns updated model</done>
</task>

<task type="auto">
  <name>Task 2: Add delete model endpoint (MODEL-04)</name>
  <files>main_fastapi.py</files>
  <action>
    Add POST endpoint at `/api/v1/admin/models/delete/{model_id}` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates model_id exists (404 if not found)
    3. Checks AppSettings for multimodal_model_id reference (warn but allow deletion)
    4. Deletes ModelConfig from database
    5. Returns success confirmation

    Response:
    ```json
    {
      "success": true,
      "message": "Model deleted successfully"
    }
    ```

    Note: Unlike providers, models can be deleted even if referenced in app settings (just log warning).

    Error handling:
    - 404 if model not found
    - 500 on database errors

    Follow Flask pattern from modules/admin_models.py lines 274-286.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8001/api/v1/admin/models/delete/1 -H "Authorization: Bearer TOKEN" | jq '.success'</automated>
  </verify>
  <done>Endpoint deletes model and returns success confirmation</done>
</task>

</tasks>

<verification>
Both endpoints respond correctly:
- POST /api/v1/admin/models/edit/{id} returns 200 with updated model
- POST /api/v1/admin/models/delete/{id} returns 200 with success message
- Update validates deployment configuration when relevant fields change
- Name uniqueness enforced on update
- Non-admin users receive 403 Forbidden
</verification>

<success_criteria>
1. Two new FastAPI endpoints exist in main_fastapi.py for model update and delete
2. All endpoints require admin authentication via get_current_admin_user
3. Model name uniqueness validated on update (when name changes)
4. Deployment validation uses modules/llm_utils functions correctly on update
5. Delete endpoint allows deletion even if model referenced in AppSettings
6. Response format matches frontend expectations (useModels.ts)
</success_criteria>

<output>
After completion, create `.planning/phases/08-backend-llm-model-language/08-02-SUMMARY.md`
</output>
