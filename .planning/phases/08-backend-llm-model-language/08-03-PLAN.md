---
phase: 08-backend-llm-model-language
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [schemas.py, main_fastapi.py]
autonomous: true
requirements: [LANG-01, LANG-02]

must_haves:
  truths:
    - "Admin can list all LLM languages"
    - "Admin can add new language with code and name"
  artifacts:
    - path: "schemas.py"
      provides: "LLM Language API schemas"
      exports: ["LLMLanguageListResponse", "LLMLanguageCreateRequest", "LLMLanguageCreateResponse", "LLMLanguageUpdateRequest", "LLMLanguageUpdateResponse", "LLMLanguageDeleteResponse"]
    - path: "main_fastapi.py"
      provides: "LLM Language list and create endpoints"
      exports: ["GET /api/v1/admin/languages", "POST /api/v1/admin/languages/add"]
  key_links:
    - from: "main_fastapi.py"
      to: "LLMLanguage model"
      via: "database query"
      pattern: "select\\(LLMLanguage\\)"
---

<objective>
Implement LLM Language schemas and base CRUD endpoints (list, create).

Purpose: Close gaps LANG-01 and LANG-02 by adding schemas and two FastAPI endpoints.

Output: 6 schema classes in schemas.py and two new FastAPI endpoints in main_fastapi.py.
</objective>

<execution_context>
@/home/mlk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/mlk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-llm-model-language/08-RESEARCH.md

Source Flask endpoints to port (modules/admin_languages.py):
- Lines 30-54: add_language() - POST /admin/languages/add (create with uniqueness check)

Key validation logic from modules/admin_languages.py:
- Language code and name are required
- Language code must be unique (IntegrityError check)
- Language name must be unique (IntegrityError check)
- Both code and name are stripped of whitespace
- is_active is boolean

Key existing FastAPI patterns (main_fastapi.py):
- Lines 720-1100: Phase 07 LLM Provider endpoints (follow same structure)
- Lines 230-240: LLMLanguage model definition in modules/models.py
- get_current_admin_user dependency for admin auth
</context>

<interfaces>
Frontend expects (from frontend/src/admin-app/src/hooks/useLanguages.ts):

GET /api/v1/admin/languages
  Response: { languages: LLMLanguage[] }
  LLMLanguage: { id, language_code, language_name, is_active, created_by, created_at }

POST /api/v1/admin/languages/add
  Request: { language_code, language_name, is_active }
  Response: { success: true, message: string, language?: LLMLanguage }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add LLM Language schemas to schemas.py</name>
  <files>schemas.py</files>
  <action>
    Add the following schema classes to schemas.py after the Model Config schemas:

    1. LLMLanguageListResponse - Response for list languages endpoint with success and data fields
    2. LLMLanguageCreateRequest - Request to create language (language_code, language_name, is_active)
    3. LLMLanguageCreateResponse - Response with success, message, and language fields
    4. LLMLanguageUpdateRequest - Request to update language (language_code, language_name, is_active)
    5. LLMLanguageUpdateResponse - Response with success, message, and language fields
    6. LLMLanguageDeleteResponse - Response with success and message fields

    Follow the exact pattern from LLMProvider schemas and ModelConfig schemas.

    Example structure:
    ```python
    class LLMLanguageListResponse(SmartLibBase):
        """Response for list languages endpoint."""
        success: bool = True
        data: Dict[str, Any] = {}  # Contains items list and total

    class LLMLanguageCreateRequest(SmartLibBase):
        """Request to create a language."""
        language_code: str
        language_name: str
        is_active: bool = True

    class LLMLanguageCreateResponse(SmartLibBase):
        """Response for create language endpoint."""
        success: bool = True
        message: str
        language: Dict[str, Any]
    ```
  </action>
  <verify>
    <automated>python3 -c "from schemas import LLMLanguageListResponse, LLMLanguageCreateRequest, LLMLanguageDeleteResponse; print('Schemas OK')"</automated>
  </verify>
  <done>All 6 language schemas exist in schemas.py and can be imported without errors</done>
</task>

<task type="auto">
  <name>Task 2: Add list languages endpoint (LANG-01)</name>
  <files>main_fastapi.py</files>
  <action>
    Add GET endpoint at `/api/v1/admin/languages` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Queries LLMLanguage table ordered by language_name
    3. Returns list of all languages with all fields

    Response format:
    ```json
    {
      "success": true,
      "data": {
        "items": [
          {
            "id": 1,
            "language_code": "en",
            "language_name": "English",
            "is_active": true,
            "created_by": "admin",
            "created_at": "2026-02-27T..."
          }
        ],
        "total": 1
      }
    }
    ```

    Follow Phase 07 pattern from main_fastapi.py list providers endpoint.
  </action>
  <verify>
    <automated>curl "http://localhost:8001/api/v1/admin/languages" -H "Authorization: Bearer TOKEN" | jq '.success'</automated>
  </verify>
  <done>Endpoint returns list of all languages ordered by name</done>
</task>

<task type="auto">
  <name>Task 3: Add create language endpoint (LANG-02)</name>
  <files>main_fastapi.py</files>
  <action>
    Add POST endpoint at `/api/v1/admin/languages/add` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates required fields: language_code, language_name (non-empty, stripped)
    3. Creates LLMLanguage with is_active=true by default
    4. Catches IntegrityError for duplicate code or name (returns 409 Conflict)
    5. Returns created language with 201 status

    Request body:
    ```json
    {
      "language_code": "en",
      "language_name": "English",
      "is_active": true
    }
    ```

    Response:
    ```json
    {
      "success": true,
      "message": "Language added successfully",
      "language": {
        "id": 1,
        "language_code": "en",
        "language_name": "English",
        "is_active": true,
        "created_by": "admin",
        "created_at": "2026-02-27T..."
      }
    }
    ```

    Error handling:
    - 400 if language_code or language_name missing
    - 409 if language_code or language_name already exists (IntegrityError)
    - 500 on database errors

    Follow Flask pattern from modules/admin_languages.py lines 30-54.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8001/api/v1/admin/languages/add -H "Authorization: Bearer TOKEN" -H "Content-Type: application/json" -d '{"language_code":"test","language_name":"Test Language"}' | jq '.success'</automated>
  </verify>
  <done>Endpoint creates language with uniqueness validation, returns created language with 201</done>
</task>

</tasks>

<verification>
Both endpoints respond correctly:
- GET /api/v1/admin/languages returns 200 with language list
- POST /api/v1/admin/languages/add returns 201 with created language
- Language code and name uniqueness enforced on create
- Non-admin users receive 403 Forbidden
</verification>

<success_criteria>
1. Six language schemas exist in schemas.py
2. Two new FastAPI endpoints exist in main_fastapi.py for language list and create
3. All endpoints require admin authentication via get_current_admin_user
4. Language code uniqueness validated (IntegrityError handling)
5. Language name uniqueness validated (IntegrityError handling)
6. Response format matches frontend expectations (useLanguages.ts)
7. Languages ordered by language_name in list response
8. created_at timestamp returned in ISO format
</success_criteria>

<output>
After completion, create `.planning/phases/08-backend-llm-model-language/08-03-SUMMARY.md`
</output>
