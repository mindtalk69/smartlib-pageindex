---
phase: 08-backend-llm-model-language
plan: 05
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: [main_fastapi.py]
autonomous: true
requirements: [MODEL-05, MODEL-06, MODEL-07]

must_haves:
  truths:
    - "Admin can set model as default"
    - "Admin can set model as multimodal"
    - "Admin can validate deployment configuration (streaming, temperature, connectivity)"
  artifacts:
    - path: "main_fastapi.py"
      provides: "Model Config action endpoints"
      exports: ["POST /api/v1/admin/models/set-default/{id}", "POST /api/v1/admin/models/set-multimodal/{id}", "POST /api/v1/admin/models/validate"]
  key_links:
    - from: "main_fastapi.py"
      to: "ModelConfig model"
      via: "database query"
      pattern: "select\\(ModelConfig\\)"
    - from: "main_fastapi.py"
      to: "AppSettings model"
      via: "update multimodal_model_id"
      pattern: "update\\(AppSettings\\)"
    - from: "main_fastapi.py"
      to: "modules/llm_utils.py"
      via: "validation helpers"
      pattern: "is_streaming_supported_for_deployment|validate_temperature_for_deployment|get_llm"
---

<objective>
Implement FastAPI action endpoints for Model Config management (set-default, set-multimodal, validate).

Purpose: Close gaps MODEL-05 through MODEL-07 by porting Flask endpoints from modules/admin_models.py to FastAPI.

Output: Three new FastAPI endpoints in main_fastapi.py for model configuration actions with admin authentication.
</objective>

<execution_context>
@/home/mlk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/mlk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-llm-model-language/08-RESEARCH.md
@.planning/phases/08-backend-llm-model-language/08-01-PLAN.md

Prerequisite: Plan 08-01 must be complete (schemas and CRUD endpoints exist).

Source Flask endpoints to port (modules/admin_models.py):
- Lines 289-302: set_default_route() - POST /admin/models/set_default/<id>
- Lines 305+: set_multimodal functionality - Set model as multimodal
- Lines 43-80: _validate_deployment_configuration() - Validate streaming, temperature, connectivity

Key validation logic from modules/admin_models.py:
- Set default clears is_default on all other models
- Set multimodal updates AppSettings table with multimodal_model_id and multimodal_deployment_name
- Validate uses modules/llm_utils functions for streaming, temperature, connectivity checks

Key existing FastAPI patterns (main_fastapi.py):
- Lines 720-1100: Phase 07 LLM Provider endpoints (follow same structure)
- get_current_admin_user dependency for admin auth
- update() statement for bulk updates (Phase 07 pattern)
</context>

<interfaces>
Frontend expects (from frontend/src/admin-app/src/hooks/useModels.ts):

POST /api/v1/admin/models/set-default/{id}
  Response: { success: true, message: string }

POST /api/v1/admin/models/set-multimodal/{id}
  Response: { success: true, message: string }

POST /api/v1/admin/models/validate
  Request: { deployment_name, temperature?, streaming, provider_id? }
  Response: { valid, streaming_supported?, temperature_valid?, connectivity_ok?, message? }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add set default model endpoint (MODEL-05)</name>
  <files>main_fastapi.py</files>
  <action>
    Add POST endpoint at `/api/v1/admin/models/set-default/{model_id}` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates model_id exists (404 if not found)
    3. Clears is_default on all other models using update statement:
       ```python
       db.exec(update(ModelConfig).where(ModelConfig.id != model_id).values(is_default=False))
       ```
    4. Sets is_default=true on target model
    5. Commits changes to database
    6. Returns success confirmation

    Response:
    ```json
    {
      "success": true,
      "message": "Default model updated"
    }
    ```

    Error handling:
    - 404 if model not found
    - 500 on database errors

    Follow Flask pattern from modules/admin_models.py lines 289-302.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8001/api/v1/admin/models/set-default/1 -H "Authorization: Bearer TOKEN" | jq '.success'</automated>
  </verify>
  <done>Endpoint sets model as default, clears other defaults, returns success confirmation</done>
</task>

<task type="auto">
  <name>Task 2: Add set multimodal model endpoint (MODEL-06)</name>
  <files>main_fastapi.py</files>
  <action>
    Add POST endpoint at `/api/v1/admin/models/set-multimodal/{model_id}` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates model_id exists (404 if not found)
    3. Gets model's deployment_name
    4. Updates AppSettings table with multimodal_model_id = model_id
    5. Also updates AppSettings with multimodal_deployment_name for reference
    6. Returns success confirmation

    Response:
    ```json
    {
      "success": true,
      "message": "Multimodal model updated"
    }
    ```

    Error handling:
    - 404 if model not found
    - 500 on database errors

    Follow Flask pattern from modules/admin_models.py (set_multimodal functionality).
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8001/api/v1/admin/models/set-multimodal/1 -H "Authorization: Bearer TOKEN" | jq '.success'</automated>
  </verify>
  <done>Endpoint sets model as multimodal in AppSettings, returns success confirmation</done>
</task>

<task type="auto">
  <name>Task 3: Add validate deployment endpoint (MODEL-07)</name>
  <files>main_fastapi.py</files>
  <action>
    Add POST endpoint at `/api/v1/admin/models/validate` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Accepts deployment_name, temperature, streaming, provider_id
    3. Validates using modules/llm_utils functions:
       - is_streaming_supported_for_deployment() -> streaming_supported
       - validate_temperature_for_deployment() -> temperature_valid
       - get_llm() test -> connectivity_ok
    4. Returns validation results with specific flags

    Response:
    ```json
    {
      "success": true,
      "valid": true,
      "streaming_supported": true,
      "temperature_valid": true,
      "connectivity_ok": true,
      "message": "Deployment configuration is valid"
    }
    ```

    If validation fails:
    ```json
    {
      "success": true,
      "valid": false,
      "streaming_supported": false,
      "temperature_valid": true,
      "connectivity_ok": null,
      "message": "Deployment 'xyz' does not support streaming"
    }
    ```

    Follow Flask pattern from modules/admin_models.py lines 43-80 (_validate_deployment_configuration).
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8001/api/v1/admin/models/validate -H "Authorization: Bearer TOKEN" -H "Content-Type: application/json" -d '{"deployment_name":"gpt-4o","streaming":true}' | jq '.valid'</automated>
  </verify>
  <done>Endpoint validates deployment configuration and returns detailed validation flags</done>
</task>

</tasks>

<verification>
All three endpoints respond correctly:
- POST /api/v1/admin/models/set-default/{id} returns 200 with success message
- POST /api/v1/admin/models/set-multimodal/{id} returns 200 with success message
- POST /api/v1/admin/models/validate returns 200 with validation results
- Set default clears is_default on all other models
- Set multimodal updates AppSettings table
- Validate endpoint returns detailed flags (streaming_supported, temperature_valid, connectivity_ok)
- Non-admin users receive 403 Forbidden
</verification>

<success_criteria>
1. Three new FastAPI endpoints exist in main_fastapi.py for model config actions
2. All endpoints require admin authentication via get_current_admin_user
3. Set default clears is_default on all other models using bulk update
4. Set multimodal updates AppSettings with multimodal_model_id and multimodal_deployment_name
5. Validate endpoint uses modules/llm_utils functions correctly
6. Validate returns detailed validation flags for each check
7. Response format matches frontend expectations (useModels.ts)
</success_criteria>

<output>
After completion, create `.planning/phases/08-backend-llm-model-language/08-05-SUMMARY.md`
</output>
