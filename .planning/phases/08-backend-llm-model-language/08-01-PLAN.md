---
phase: 08-backend-llm-model-language
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [schemas.py, main_fastapi.py]
autonomous: true
requirements: [MODEL-01, MODEL-02]

must_haves:
  truths:
    - "Admin can list all models with provider association"
    - "Admin can add new model with deployment validation"
  artifacts:
    - path: "schemas.py"
      provides: "Model Config API schemas"
      exports: ["ModelConfigListResponse", "ModelConfigCreateRequest", "ModelConfigCreateResponse", "ModelConfigUpdateRequest", "ModelConfigUpdateResponse", "ModelConfigDeleteResponse", "ModelConfigDefaultResponse", "ModelConfigMultimodalResponse", "ModelValidationRequest", "ModelValidationResponse"]
    - path: "main_fastapi.py"
      provides: "Model Config list and create endpoints"
      exports: ["GET /api/v1/admin/models", "POST /api/v1/admin/models/add"]
  key_links:
    - from: "main_fastapi.py"
      to: "ModelConfig model"
      via: "database query"
      pattern: "select\\(ModelConfig\\)"
    - from: "main_fastapi.py"
      to: "modules/llm_utils.py"
      via: "validation helpers"
      pattern: "is_streaming_supported_for_deployment|validate_temperature_for_deployment|get_llm"
---

<objective>
Implement Model Config schemas and base CRUD endpoints (list, create).

Purpose: Close gaps MODEL-01 and MODEL-02 by adding schemas and two FastAPI endpoints.

Output: 10 schema classes in schemas.py and two new FastAPI endpoints in main_fastapi.py.
</objective>

<execution_context>
@/home/mlk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/mlk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-llm-model-language/08-RESEARCH.md

Key validation logic from modules/admin_models.py:
- Lines 28-40: _coerce_temperature() - Convert temperature to float
- Lines 43-80: _validate_deployment_configuration() - Validate streaming, temperature, connectivity
- Uses modules/llm_utils: is_streaming_supported_for_deployment(), validate_temperature_for_deployment(), get_llm()

Key existing FastAPI patterns (main_fastapi.py):
- Lines 720-1100: Phase 07 LLM Provider endpoints (follow same structure)
- Lines 184-204: ModelConfig model definition in modules/models.py
- get_current_admin_user dependency for admin auth
</context>

<interfaces>
Frontend expects (from frontend/src/admin-app/src/hooks/useModels.ts):

GET /api/v1/admin/models
  Response: { models?: ModelConfig[] }
  ModelConfig: { id, provider_id, name, deployment_name, provider, temperature, streaming, description, is_default, is_multimodal, created_by, created_at, provider_obj }

POST /api/v1/admin/models/add
  Request: { name, deployment_name, provider_id, temperature, streaming, description, is_default }
  Response: { success: true, model: ModelConfig }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Model Config schemas to schemas.py</name>
  <files>schemas.py</files>
  <action>
    Add the following schema classes to schemas.py after the existing LLMProvider schemas (around line 225):

    1. ModelConfigListResponse - Response for list models endpoint with success and data fields
    2. ModelConfigCreateRequest - Request to create model (name, deployment_name, provider_id, temperature, streaming, description, is_default)
    3. ModelConfigCreateResponse - Response with success and model fields
    4. ModelConfigUpdateRequest - Request to update model (all fields optional)
    5. ModelConfigUpdateResponse - Response with success and model fields
    6. ModelConfigDeleteResponse - Response with success and message fields
    7. ModelConfigDefaultResponse - Response for set default with success and message
    8. ModelConfigMultimodalResponse - Response for set multimodal with success and message
    9. ModelValidationRequest - Request for validation (deployment_name, temperature, streaming, provider_id)
    10. ModelValidationResponse - Response with valid, streaming_supported, temperature_valid, connectivity_ok, message

    Follow the exact pattern from LLMProvider schemas (lines 145-225) and the research document examples.
  </action>
  <verify>
    <automated>python3 -c "from schemas import ModelConfigListResponse, ModelConfigCreateRequest, ModelConfigValidationRequest; print('Schemas OK')"</automated>
  </verify>
  <done>All 10 model config schemas exist in schemas.py and can be imported without errors</done>
</task>

<task type="auto">
  <name>Task 2: Add list models endpoint (MODEL-01)</name>
  <files>main_fastapi.py</files>
  <action>
    Add GET endpoint at `/api/v1/admin/models` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Queries ModelConfig table with associated LLMProvider using join or separate query
    3. Returns list of models with provider_obj field containing {id, name, provider_type, is_active}
    4. Orders results by name for consistent display

    Response format:
    ```json
    {
      "success": true,
      "data": {
        "items": [
          {
            "id": 1,
            "provider_id": 1,
            "name": "gpt-4o",
            "deployment_name": "gpt-4o",
            "provider": "azure_openai",
            "temperature": 0.7,
            "streaming": true,
            "description": "GPT-4 Optimized",
            "is_default": true,
            "is_multimodal": false,
            "created_by": "admin",
            "created_at": "2026-02-27T...",
            "provider_obj": {
              "id": 1,
              "name": "Azure OpenAI",
              "provider_type": "azure_openai",
              "is_active": true
            }
          }
        ],
        "total": 1
      }
    }
    ```

    Follow Phase 07 pattern from main_fastapi.py list providers endpoint.
  </action>
  <verify>
    <automated>curl "http://localhost:8001/api/v1/admin/models" -H "Authorization: Bearer TOKEN" | jq '.success'</automated>
  </verify>
  <done>Endpoint returns list of all models with provider_obj association, ordered by name</done>
</task>

<task type="auto">
  <name>Task 3: Add create model endpoint (MODEL-02)</name>
  <files>main_fastapi.py</files>
  <action>
    Add POST endpoint at `/api/v1/admin/models/add` that:
    1. Requires admin authentication via `Depends(get_current_admin_user)`
    2. Validates required fields: name, deployment_name, provider_id
    3. Gets provider object to validate provider_id exists
    4. Imports and uses from modules/llm_utils:
       - is_streaming_supported_for_deployment() - validates streaming flag
       - validate_temperature_for_deployment() - validates temperature range
       - get_llm() - tests connectivity
    5. Checks for duplicate model name (returns 400 if exists)
    6. Clears other defaults if is_default=true
    7. Creates ModelConfig with provider_type from provider_obj
    8. Returns created model with 201 status

    Request body:
    ```json
    {
      "name": "gpt-4o",
      "deployment_name": "gpt-4o",
      "provider_id": 1,
      "temperature": 0.7,
      "streaming": true,
      "description": "GPT-4 Optimized",
      "is_default": true
    }
    ```

    Error handling:
    - 400 if required fields missing
    - 400 if provider not found
    - 400 if streaming not supported for deployment
    - 400 if temperature out of range for deployment
    - 400 if connectivity test fails
    - 400 if duplicate name
    - 500 on database errors

    Follow Flask pattern from modules/admin_models.py lines 120-181.
  </action>
  <verify>
    <automated>curl -X POST http://localhost:8001/api/v1/admin/models/add -H "Authorization: Bearer TOKEN" -H "Content-Type: application/json" -d '{"name":"Test Model","deployment_name":"gpt-4o","provider_id":1}' | jq '.success'</automated>
  </verify>
  <done>Endpoint creates model with full deployment validation, prevents duplicates, returns created model with 201</done>
</task>

</tasks>

<verification>
Both endpoints respond correctly:
- GET /api/v1/admin/models returns 200 with model list including provider_obj
- POST /api/v1/admin/models/add returns 201 with created model
- Create validates deployment configuration (streaming, temperature, connectivity)
- Name uniqueness enforced on create
- Non-admin users receive 403 Forbidden
</verification>

<success_criteria>
1. Ten model config schemas exist in schemas.py
2. Two new FastAPI endpoints exist in main_fastapi.py for model list and create
3. All endpoints require admin authentication via get_current_admin_user
4. Model name uniqueness validated on create
5. Deployment validation uses modules/llm_utils functions correctly
6. Response format matches frontend expectations (useModels.ts)
7. provider_obj included in list response for frontend display
</success_criteria>

<output>
After completion, create `.planning/phases/08-backend-llm-model-language/08-01-SUMMARY.md`
</output>
