---
phase: 09-content-management-settings
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [schemas.py, main_fastapi.py, modules/models.py]
autonomous: true
requirements: [CONTENT-06, CONTENT-07]

must_haves:
  truths:
    - "Admin can manage catalogs (list, create, update, delete)"
    - "Admin can manage categories (list, create, update, delete)"
  artifacts:
    - path: "modules/models.py"
      provides: "SQLModel models for Catalog and Category"
      exports: ["Catalog", "Category"]
    - path: "schemas.py"
      provides: "Catalog and Category API schemas"
      exports: ["CatalogListResponse", "CatalogCreateRequest", "CatalogCreateResponse", "CatalogUpdateRequest", "CatalogUpdateResponse", "CatalogDeleteResponse", "CategoryListResponse", "CategoryCreateRequest", "CategoryCreateResponse", "CategoryUpdateRequest", "CategoryUpdateResponse", "CategoryDeleteResponse"]
    - path: "main_fastapi.py"
      provides: "Catalog and Category CRUD endpoints"
      exports: ["GET /api/v1/admin/catalogs", "POST /api/v1/admin/catalogs/add", "POST /api/v1/admin/catalogs/edit/{catalog_id}", "DELETE /api/v1/admin/catalogs/delete/{catalog_id}", "GET /api/v1/admin/categories", "POST /api/v1/admin/categories/add", "POST /api/v1/admin/categories/edit/{category_id}", "DELETE /api/v1/admin/categories/delete/{category_id}"]
  key_links:
    - from: "modules/models.py"
      to: "Catalog model"
      via: "SQLModel definition"
      pattern: "class Catalog\\(SQLModel"
    - from: "modules/models.py"
      to: "Category model"
      via: "SQLModel definition"
      pattern: "class Category\\(SQLModel"
    - from: "main_fastapi.py"
      to: "Catalog CRUD"
      via: "FastAPI endpoints"
      pattern: "/api/v1/admin/catalogs"
    - from: "main_fastapi.py"
      to: "Category CRUD"
      via: "FastAPI endpoints"
      pattern: "/api/v1/admin/categories"
---

<objective>
Implement Catalog and Category CRUD endpoints.

Purpose: Close gaps CONTENT-06, CONTENT-07 by adding SQLModel definitions, schemas, and FastAPI endpoints for catalog and category management.

Output: 2 SQLModel models in modules/models.py, 12 schema classes in schemas.py, and 8 new FastAPI endpoints in main_fastapi.py.
</objective>

<execution_context>
@/home/mlk/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/mlk/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-content-management-settings/09-RESEARCH.md

Key Flask reference (modules/admin_catalogs.py):
- Lines 21-49: add_catalog() - Create catalog with name uniqueness validation
- Lines 51-65: get_catalog_data() - Get single catalog
- Lines 67-94: edit_catalog() - Update catalog with IntegrityError handling
- Lines 96-108: delete_catalog_route() - Delete catalog

Key SQLAlchemy models (modules/database.py):
- Lines 221-230: Category SQLAlchemy model
- Lines 232-241: Catalog SQLAlchemy model

**IMPORTANT:** Catalog and Category currently exist only as SQLAlchemy models in modules/database.py. For FastAPI consistency, we need to add SQLModel versions to modules/models.py first, then use those for the endpoints.

Key existing patterns from Phase 07-08 (main_fastapi.py):
- Lines 1761-1900: LLMLanguage CRUD endpoints - Follow this exact pattern
- Uniqueness handling via IntegrityError catch (Phase 08)
- get_current_admin_user dependency for admin auth
</context>

<interfaces>
Frontend expects (new hooks to be created in Phase 09):

GET /api/v1/admin/catalogs
  Response: { success: true, data: { items: [...], total: 5 } }
  Item: { id, name, description, created_by, created_at }

POST /api/v1/admin/catalogs/add
  Request: { name, description }
  Response: { success: true, message: "...", catalog: {...} }

POST /api/v1/admin/catalogs/edit/{catalog_id}
  Request: { name, description }
  Response: { success: true, message: "...", catalog: {...} }

DELETE /api/v1/admin/catalogs/delete/{catalog_id}
  Response: { success: true, message: "..." }

(Same pattern for categories with /api/v1/admin/categories)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Catalog and Category SQLModel models to modules/models.py</name>
  <files>modules/models.py</files>
  <action>
    Add the following SQLModel classes to modules/models.py after the AppSettings model (around line 230):

    1. Catalog - SQLModel table matching SQLAlchemy schema
       - __tablename__ = "catalogs"
       - id: Optional[int] = Field(default=None, primary_key=True)
       - name: str = Field(unique=True, nullable=False)
       - description: Optional[str] = Field(default=None)
       - created_by_user_id: str = Field(foreign_key="users.user_id")
       - created_at: datetime = Field(default_factory=datetime.utcnow, sa_column=Column(DateTime, server_default=func.now()))

    2. Category - SQLModel table matching SQLAlchemy schema
       - __tablename__ = "categories"
       - id: Optional[int] = Field(default=None, primary_key=True)
       - name: str = Field(unique=True, nullable=False)
       - description: Optional[str] = Field(default=None)
       - created_by_user_id: str = Field(foreign_key="users.user_id")
       - created_at: datetime = Field(default_factory=datetime.utcnow, sa_column=Column(DateTime, server_default=func.now()))

    Follow the exact pattern from LLMLanguage model (modules/models.py lines 230-245).
  </action>
  <verify>
    <automated>python3 -c "from modules.models import Catalog, Category; print('Models OK')"</automated>
  </verify>
  <done>Catalog and Category SQLModel models exist in modules/models.py and can be imported without errors</done>
</task>

<task type="auto">
  <name>Task 2: Add Catalog and Category schemas to schemas.py</name>
  <files>schemas.py</files>
  <action>
    Add the following schema classes to schemas.py after the file management schemas (around line 325):

    Catalog schemas (6 classes):
    1. CatalogListResponse - Response for catalog list endpoint with success and data fields
    2. CatalogCreateRequest - Request to create catalog (name, description)
    3. CatalogCreateResponse - Response with success, message, and catalog fields
    4. CatalogUpdateRequest - Request to update catalog (name, description)
    5. CatalogUpdateResponse - Response with success, message, and catalog fields
    6. CatalogDeleteResponse - Response with success and message fields

    Category schemas (6 classes):
    7. CategoryListResponse - Response for category list endpoint with success and data fields
    8. CategoryCreateRequest - Request to create category (name, description)
    9. CategoryCreateResponse - Response with success, message, and category fields
    10. CategoryUpdateRequest - Request to update category (name, description)
    11. CategoryUpdateResponse - Response with success, message, and category fields
    12. CategoryDeleteResponse - Response with success and message fields

    Follow the exact pattern from LLMLanguage schemas (lines 285-325).
  </action>
  <verify>
    <automated>python3 -c "from schemas import CatalogCreateRequest, CategoryCreateRequest; print('Schemas OK')"</automated>
  </verify>
  <done>All 12 catalog and category schemas exist in schemas.py and can be imported without errors</done>
</task>

<task type="auto">
  <name>Task 3: Add Catalog CRUD endpoints (CONTENT-06)</name>
  <files>main_fastapi.py</files>
  <action>
    Add 4 Catalog endpoints at `/api/v1/admin/catalogs` following the LLMLanguage pattern (lines 1761-1850):

    1. GET /api/v1/admin/catalogs (list)
       - Requires admin auth
       - Queries all Catalog ordered by name
       - Returns items with: id, name, description, created_by, created_at
       - Returns total count

    2. POST /api/v1/admin/catalogs/add (create)
       - Requires admin auth
       - Validates name is not empty
       - Checks for duplicate name (select where name matches)
       - Returns 400 if duplicate exists
       - Creates Catalog with created_by_user_id from current_user
       - Returns 201 with created catalog

    3. POST /api/v1/admin/catalogs/edit/{catalog_id} (update)
       - Requires admin auth
       - Gets catalog by id, returns 404 if not found
       - Validates name is not empty
       - Checks for duplicate name (excluding current id)
       - Returns 409 if duplicate exists
       - Updates name and description
       - Returns updated catalog

    4. DELETE /api/v1/admin/catalogs/delete/{catalog_id} (delete)
       - Requires admin auth
       - Gets catalog by id, returns 404 if not found
       - Deletes catalog
       - Returns success message

    Follow the exact pattern from LLMLanguage endpoints in main_fastapi.py lines 1761-1850.

    Uniqueness handling pattern:
    ```python
    # Check for duplicate name
    existing = db.exec(select(Catalog).where(Catalog.name == name)).first()
    if existing:
        raise HTTPException(status_code=400, detail=f"Catalog with name '{name}' already exists.")

    # For update, exclude current id
    existing = db.exec(
        select(Catalog).where(
            Catalog.name == name,
            Catalog.id != catalog_id
        )
    ).first()
    if existing:
        raise HTTPException(status_code=409, detail=f"Catalog with name '{name}' already exists.")
    ```
  </action>
  <verify>
    <automated>curl "http://localhost:8001/api/v1/admin/catalogs" -H "Authorization: Bearer TOKEN" | jq '.success'</automated>
  </verify>
  <done>All 4 catalog endpoints exist and work correctly: list, create, update, delete</done>
</task>

<task type="auto">
  <name>Task 4: Add Category CRUD endpoints (CONTENT-07)</name>
  <files>main_fastapi.py</files>
  <action>
    Add 4 Category endpoints at `/api/v1/admin/categories` following the exact same pattern as Catalog endpoints:

    1. GET /api/v1/admin/categories (list)
       - Requires admin auth
       - Queries all Category ordered by name
       - Returns items with: id, name, description, created_by_user_id, created_at
       - Returns total count

    2. POST /api/v1/admin/categories/add (create)
       - Requires admin auth
       - Validates name is not empty
       - Checks for duplicate name
       - Returns 400 if duplicate exists
       - Creates Category with created_by_user_id from current_user
       - Returns 201 with created category

    3. POST /api/v1/admin/categories/edit/{category_id} (update)
       - Requires admin auth
       - Gets category by id, returns 404 if not found
       - Validates name is not empty
       - Checks for duplicate name (excluding current id)
       - Returns 409 if duplicate exists
       - Updates name and description
       - Returns updated category

    4. DELETE /api/v1/admin/categories/delete/{category_id} (delete)
       - Requires admin auth
       - Gets category by id, returns 404 if not found
       - Deletes category
       - Returns success message

    Follow the exact pattern from Catalog endpoints (just created in Task 3).

    Response formats identical to Catalog, just replace "catalog" with "category".
  </action>
  <verify>
    <automated>curl "http://localhost:8001/api/v1/admin/categories" -H "Authorization: Bearer TOKEN" | jq '.success'</automated>
  </verify>
  <done>All 4 category endpoints exist and work correctly: list, create, update, delete</done>
</task>

</tasks>

<verification>
All endpoints respond correctly:
Catalog endpoints:
- GET /api/v1/admin/catalogs returns 200 with catalog list
- POST /api/v1/admin/catalogs/add returns 201 with created catalog
- POST /api/v1/admin/catalogs/edit/{id} returns 200 with updated catalog
- DELETE /api/v1/admin/catalogs/delete/{id} returns 200 with success message

Category endpoints:
- GET /api/v1/admin/categories returns 200 with category list
- POST /api/v1/admin/categories/add returns 201 with created category
- POST /api/v1/admin/categories/edit/{id} returns 200 with updated category
- DELETE /api/v1/admin/categories/delete/{id} returns 200 with success message

Common behaviors:
- Name uniqueness enforced (400/409 on duplicates)
- Non-existent resources return 404
- Non-admin users receive 403 Forbidden
- All operations use get_current_admin_user dependency
</verification>

<success_criteria>
1. Two new SQLModel models exist in modules/models.py (Catalog, Category)
2. 12 new schema classes exist in schemas.py (6 each for catalogs and categories)
3. 8 new FastAPI endpoints exist in main_fastapi.py (4 each)
4. All endpoints require admin authentication
5. Name uniqueness validated for both catalogs and categories
6. Update operations exclude current id from duplicate check
7. Response format matches frontend expectations
8. Follows Phase 07-08 patterns exactly
</success_criteria>

<output>
After completion, create `.planning/phases/09-content-management-settings/09-03-SUMMARY.md`
</output>
