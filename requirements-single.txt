# ============================================================================
# SmartLib BASIC - Single Container Requirements
# Minimal dependencies for web + lightweight worker
# No heavy local ML, no Redis, no PostgreSQL
# Target: ~250-300MB
# ============================================================================

# Core Web Framework
Flask==3.1.0
Flask-Login==0.6.3
Flask-Migrate==4.1.0
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
gunicorn==23.0.0
Werkzeug==3.1.3

# Celery (SQLite backend - no Redis needed)
celery==5.5.2

# Database
SQLAlchemy==2.0.39
alembic==1.15.2

# OpenAI API (primary embedding method - NO local ML)
openai==1.100.2
tiktoken==0.9.0
langchain-openai==0.2.10

# LangChain Core (minimal)
langchain>=0.3.18
langchain-community>=0.3.17
langgraph-sdk>=0.1.66

# Vector Store - sqlite-vec
sqlite-vec>=0.1.1

# HTTP & Utilities
httpx==0.28.1
requests==2.32.3
python-dotenv==1.0.1
pydantic==2.11.4

# Document loading (lightweight reader only)
docling-core~=2.29

# MS Authentication
msal==1.30.0

# Essential dependencies
lark==1.2.2
Pillow==11.0.0

# ============================================================================
# EXCLUDED for size optimization:
# - sentence-transformers (~200MB) - use API embeddings
# - langchain-huggingface - replaced by langchain-community
# - scikit-learn (~50MB) - not needed for basic RAG
# - PyMuPDF (~50MB) - delegate to API
# - pdfminer.six (~20MB) - delegate to API
# - torch, torchvision (~200MB) - not needed
# - numpy, scipy, pandas (~150MB) - not needed for basic
# ============================================================================
