# ============================================================================
# WEB ENTERPRISE REQUIREMENTS.TXT - Target: ~200-300MB footprint
# Flask web-only container for Enterprise edition with PostgreSQL/pgvector
# ============================================================================

# Core Web Framework
Flask==3.1.0
Flask-Login==0.6.3
Flask-Migrate==4.1.0
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
gunicorn>=20.1.0
Werkzeug==3.1.3

# Celery Client (minimal - just to submit tasks)
celery==5.5.2
redis==5.0.1

# Database (PostgreSQL for Enterprise)
SQLAlchemy==2.0.39
alembic==1.15.2
psycopg[binary]==3.1.18

# OpenAI API (primary embedding method - NO local ML)
openai==1.100.2
tiktoken==0.9.0
langchain-openai==0.2.10
langchain>=0.3.18
langchain-community>=0.3.17
langchain-postgres>=0.0.6
langgraph-checkpoint>=2.0.25
langgraph-prebuilt>=0.1.8
langgraph-supervisor>=0.0.19
langgraph-sdk>=0.1.66
pgvector>=0.2.5

# Document loading handled by worker container (remote API only)
# Visual grounding preview requires lightweight Docling reader

docling-core~=2.29
# Strictly limit to datamodel loader; skips heavy OCR extras
# docling meta package pulls converters we don't need on web

# HTTP & Utilities (minimal)
httpx==0.28.1
requests==2.32.3
python-dotenv==1.0.1
pydantic==2.11.4

# Microsoft Authentication (needed for MS login)
msal==1.30.0

# Vector database interactions handled via worker microservice

# Additional dependencies
lark==1.2.2
Pillow==11.0.0
folium==0.18.0
geopy==2.4.1
langgraph>=0.2.60
langchain-experimental>=0.0.65
selenium>=4.25.0
webdriver-manager>=4.0.1
matplotlib>=3.9.0
pandas==2.2.3

# Experimental dependencies for agent tools are loaded in worker image

# ============================================================================
# ENTERPRISE EDITION CHANGES:
# ============================================================================
# - Removed chromadb and langchain-chroma (uses pgvector instead)
# - Added psycopg for PostgreSQL connectivity
# - Faster build time (no ChromaDB HNSW compilation)
# - All vector operations use PostgreSQL with pgvector extension
#
# Expected size: ~200-300MB
# - Supports: text-embedding-3-small (API-based)
# - Delegates document parsing and local embeddings to worker container
# ============================================================================
